<?php

if ( ! class_exists( 'SJQpnHelper' ) ) {
	
	/**
	 * Responsible for setting up Helper Functions.
	 *
	 * @since 1.0
	 */
	final class SJQpnHelper {
		
		/**
		 * Load the builder if it's not already loaded, otherwise
		 * show an admin notice.
		 *
		 * @since 1.0
		 * @return void
		 */ 
		// static public function init() {
		// 	self::define_constants();
		// 	self::load_files();
		// }
		
		/**
		 * Define builder constants.
		 *
		 * @since 1.0
		 * @return void
		 */ 
		static public function create_menu( $args = array(), $type = 'page' ) {
			
		}
	}
}